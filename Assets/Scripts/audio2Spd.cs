using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class audio2Spd : MonoBehaviour
{
    public AudioSource audioSorce;
    
    // Start is called before the first frame update
    void Start()
    {
        //audio
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
